const config={animationDelay:400,skillAnimationDelay:100,observerThreshold:0.1,observerRootMargin:'50px'};const animationObserver=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){requestAnimationFrame(()=>{entry.target.classList.add('animacion-activa')});animationObserver.unobserve(entry.target)}})},{threshold:config.observerThreshold,rootMargin:config.observerRootMargin});function handleCurriculumTransition(){const pantallaInicio=document.getElementById('pantallaInicio');const curriculum=document.getElementById('curriculum');if(!pantallaInicio||!curriculum)return;pantallaInicio.classList.add('oculta');requestAnimationFrame(()=>{setTimeout(()=>{curriculum.classList.add('mostrar');animateSkillBars();initAnimations()},config.animationDelay)})} function animateSkillBars(){const barrasHabilidad=document.querySelectorAll('.nivel-habilidad');barrasHabilidad.forEach((barra,index)=>{setTimeout(()=>{const ancho=barra.style.width;if(ancho){barra.style.width='0';requestAnimationFrame(()=>{barra.style.width=ancho})}},index*config.skillAnimationDelay)})} function initAnimations(){const elementosAnimados=document.querySelectorAll('.animacion-aparecer');elementosAnimados.forEach(elemento=>{animationObserver.observe(elemento)})} function initApp(){const verCurriculumBtn=document.getElementById('verCurriculum');if(verCurriculumBtn){verCurriculumBtn.addEventListener('click',handleCurriculumTransition);verCurriculumBtn.addEventListener('touchstart',handleCurriculumTransition,{passive:!0})} initAnimations()} function handleKeyboardNavigation(event){if(event.key==='Enter'||event.key===' '){const activeElement=document.activeElement;if(activeElement.id==='verCurriculum'||activeElement===document.body){event.preventDefault();handleCurriculumTransition()}}} function preventZoom(event){if(event.target.tagName==='INPUT'||event.target.tagName==='TEXTAREA'||event.target.isContentEditable){return} if(event.touches.length>1){event.preventDefault()}} document.addEventListener('keydown',handleKeyboardNavigation);document.addEventListener('touchstart',preventZoom,{passive:!1});if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',initApp)}else{initApp()}